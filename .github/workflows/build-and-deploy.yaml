name: "Build and deploy"

on:
  push:
  workflow_dispatch:

jobs:
  next-app:
    permissions:
      contents: "read"
      id-token: "write"
      packages: "write"
    uses: navikt/teamesyfo-github-actions-workflows/.github/workflows/next-app-v2.yaml@main
    secrets: inherit
    with:
      app: oppfolgingsplan-lps-demo
      base-path: /oppfolgingsplan-lps
      node-version: 24.x
